name: Update File
on:
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: wow-actions/download-upload@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          url: https://qbitmirror.eu.org/0:/Riptide%20%20Gp2%20By%20Kannan%20Mass%20Games.exe
          dir: anonfilesworkflowtestt
          filename: Riptide Gp2 By Kannan Mass Games.exe
